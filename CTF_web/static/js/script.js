document.addEventListener('DOMContentLoaded', () => {
    const message = document.getElementById('message');
    message.textContent = "Hint: Sensitive directories often use common names like '/admin' or '/backup'.";
});
